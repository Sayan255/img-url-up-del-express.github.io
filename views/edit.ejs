<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Index</title>
</head>

<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a class="text-blue-500" href="/read">Read users</a>

        </div>
        <h1 class="text-3xl tracking-tighter mb-3">Update User</h1>

        <form action="/update/<%= user._id %>" method="post">
            <input class="px-5 py-2 bg-transparent border-2 outline-none border-zinc-800 rounded-lg" type="text"
                name="name" placeholder="name" value="<%= user.name %>">
            <input class="px-5 py-2 bg-transparent border-2 outline-none border-zinc-800 rounded-lg" type="email"
                name="email" placeholder="email" value="<%= user.email %>">
            <input class="px-5 py-2 bg-transparent border-2 outline-none border-zinc-800 rounded-lg" type="image-url"
                name="image" placeholder="image-url" value="<%= user.image %>">
            <input class="px-5 py-2 bg-yellow-500  rounded-lg cursor-pointer ml-3 " type="submit" value="Update User">
        </form>
    </div>
</body>

</html>